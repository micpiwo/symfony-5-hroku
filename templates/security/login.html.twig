{% extends 'base.html.twig' %}

{% block title %}SF5 -CONNEXION-{% endblock %}

{% block body %}
    <div class="mt-5 jumbotron">
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            Bienvenue : {{ app.user.username }} : <a href="{{ path('app_logout') }}" class="btn btn-outline-warning">Deconnexion</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-3 font-weight-normal text-info">ESPACE D'ADMINISTRATION</h1>
    <label for="inputEmail">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
    <label for="inputPassword">Mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-outline-warning mt-3" type="submit">
       Connexion
    </button>
    <a href="{{ path('register') }}" class="btn btn-lg btn-outline-danger mt-3">S'inscrire</a>
    <hr>
    <em>Si vous êtes un nouvel utilisateur, merci de vous inscrire !</em>
    <hr>
</form>
    </div>

{% endblock %}
