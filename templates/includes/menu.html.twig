<nav class="navbar navbar-expand-lg navbar-light">
    <a class="navbar-brand" href="{{ path('produits') }}">
        <img width="70%"  src="{{ asset('img/sf_logo.png') }}" alt="shop" title="Symfony">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('produits') }}"><b>PRODUITS</b><span class="sr-only">(current)</span></a>
            </li>

            <li class="nav-item">
                <a class="nav-link" href="{{ path('ajouter_produit') }}"><b>AJOUTER UN PRODUIT</b></a>
            </li>

            <li class="nav-item active">
                <a class="nav-link" href="{{ path('liste_distributeurs') }}"><b>LISTE DES DISTRIBUTEURS</b></a>
            </li>

            <li class="nav-item active">
                <a class="nav-link" href=""><b>TEST</b></a>
            </li>
            {% if app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}"><b>DECONNEXION</b></a>
                </li>
            {% elseif not app.user %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}"><b>CONNEXION</b></a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>